# High-Quality Advertisement Player Requirements
# Python 3.8+ recommended

# GUI Framework - Qt5 for professional GUI with hardware acceleration
PyQt5>=5.15.9

# Computer Vision - Full OpenCV with GUI support (NOT headless)
# Note: Use opencv-python (not opencv-python-headless) for video display
opencv-python>=4.8.0

# Image Processing - High-quality image manipulation
Pillow>=10.0.0

# Numerical Computing - Array operations for video frames
numpy>=1.24.0

# Audio/Video Synchronization - FFmpeg Python bindings for unified A/V decoding
# This provides PERFECT synchronization by decoding both streams together
ffpyplayer>=4.3.5

# GStreamer Python bindings - Hardware-accelerated video playback (RECOMMENDED for Raspberry Pi)
# Provides GPU decoding, better performance, and lower CPU usage
# Install system packages: sudo apt-get install python3-gst-1.0 gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-tools
# For Raspberry Pi 4/5: sudo apt-get install gstreamer1.0-libav
# For Raspberry Pi 3: sudo apt-get install gstreamer1.0-omx
PyGObject>=3.42.0
